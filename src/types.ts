export interface inputValueData {
    id: string
    inputHeaderText: string
    defaultValue?: any
    inputType?: string
    placeholder?: string
    name: string
}

export interface diffYear {
    difference: number; 
    isFuture: boolean
}